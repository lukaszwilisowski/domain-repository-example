{
  "scripts": {
    "build": "tsc",
    "test": "jest --config ./test/jest.unit.config.json",
    "start": "ts-node-dev --respawn --transpile-only ./src/app.ts"
  },
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "jest": "^28.1.2",
    "jest-mock": "^29.3.1",
    "pg": "^8.8.0",
    "ts-jest": "^28.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "domain-repository": "^2.0.2",
    "mongoose": "^7.4.2"
  }
}
